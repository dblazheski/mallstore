package domain.service;

import domain.model.customer.Customer;
import domain.model.order.Order;
import domain.model.product.Product;

/**
 * Created by DeKi on 1/31/2016.
 */
public class OrderService {

    /**
     * Check the ordered items for qty;
     * if product have qty, then change it
     *
     *
     * @param order
     * @throws Exception
     */
    public void makeOrder(Order order) throws Exception {
//        Customer customer = customerDao.getCustomer(order.getCustomerId());
//        Order customerOrders = order.getOrdersFromCustomer(order.getCustomerId());
//        customerOrders.addOrder(order);
//        OrderDao.addOrder(customerOrders);
    }
}
